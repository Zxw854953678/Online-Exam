
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>在线考试</title>
  <link rel="stylesheet" href="/utils/bootstrap/bootstrap.min.css">
  <link rel="stylesheet" href="/css/homePage.css">
  <script src="/utils/jquery/jquery-3.2.1.min.js"></script>
  <script src="/utils/bootstrap/bootstrap.min.js"></script>
  <script src="/js/homePage.js"></script>
</head>
<body>
<header class="navbar">
  <div class="container-fluid">
    <div class="navbar-header">
      <span class="navbar-brand">在线考试</span>
      <button id="btn" type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-list">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="navbar-list">
      <ul class="nav navbar-nav">
        {{#asideMain}}
          <li>
            <a href="{{router}}">
              <span class="glyphicon {{icon}}"></span>
              <span class="content">{{content}}</span>
            </a>
          </li>
        {{/asideMain}}
      </ul>
      {{# if name}}
        <div class="nav navbar-nav navbar-right logout">
          {{name}}
          <a href="/logout">登出</a>
        </div>
      {{/if}}
    </div>
  </div>
</header>
<aside class="container-fluid p15" id="aside">
  <div class="row">
    <div class="col-sm-3 col-md-2 divList">
      <ul class="nav nav-pills nav-stacked" id="list">
        {{#asideMain}}
          <li>
            <a href="{{router}}">
              <span class="glyphicon {{icon}}"></span>
              <span class="content">{{content}}</span>
            </a>
          </li>
        {{/asideMain}}
      </ul>
    </div>
    <div class="col-xs-10 col-xs-offset-1 col-sm-9 col-sm-offset-0 col-md-10 col-md-offset-0 lookExam">
      <h3>{{current}}</h3>
      <ol class="breadcrumb">
        <li><a href="">Home</a></li>
        <li class="active">{{current}}</li>
      </ol>

      <div class="table-responsive">
        <table class="table table-condensed">
          <tr>
            {{#if isTeacher}}
              <th>学号</th>
              <th>学生姓名</th>
            {{/if}}
            {{#tableName}}
              <th>{{this}}</th>
            {{/tableName}}
          </tr>
          {{#exam}}
            <tr>
              {{#if sid}}
                <td>{{sid}}</td>
              {{/if}}
              {{#if sname}}
                <td>{{sname}}</td>
              {{/if}}
              <td>{{title}}</td>
              <td>{{createTime}}</td>
              <td>{{score}}</td>
              {{#if sid}}
                <td><a href="/index/details/{{id}}/{{sid}}">详情</a></td>
                {{else}}
                <td><a href="/index/details/{{id}}">详情</a></td>
              {{/if}}
            </tr>
          {{/exam}}
        </table>
      </div>

    </div>
  </div>
</aside>

</body>
</html>





















